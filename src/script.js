//#region -- IMPORTS
import { updateSkills } from "globalthis/implementation";
//#endregion

//#region -- SCROLL ANIMATION
const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {

    });
});

const hiddenElements = document.querySelectorAll('.hidden');
//#endregion